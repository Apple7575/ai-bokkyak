-- 1회 복용량 (C-09 확정 시안: 1정 / 2정 / 1포)
--
-- 왜 텍스트인가: "1정", "1포", "5mL"처럼 단위가 약마다 다르다. 숫자+단위로 쪼개면
-- 입력이 번거로워지고 어르신이 고를 항목만 늘어난다. 화면은 정해진 몇 개만
-- 보여주고, 저장은 표시 문자열 그대로 한다.
--
-- 기존 행은 null로 남는다 — 화면에서는 복용량을 표시하지 않는다(있으면 표시).
alter table schedules add column if not exists dose_amount text;
